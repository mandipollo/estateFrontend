import styled from "@emotion/styled";
import { TextField } from "@mui/material";

const StyledTextfieldSelect = styled(TextField)(({ theme }) => ({
	width: "100%",
}));

export default StyledTextfieldSelect;
