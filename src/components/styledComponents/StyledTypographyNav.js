import styled from "@emotion/styled";
import { Typography } from "@mui/material";

const StyledTypographyNav = styled(Typography)(() => ({
	fontWeight: 500,
	variant: "body1",
}));

export default StyledTypographyNav;
