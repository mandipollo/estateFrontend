import { createTheme } from "@mui/material";

const theme = createTheme({
	palette: {
		primary: {
			main: "#FFFFF",
		},
		hover: {
			main: "#F2F3F4",
			secondary: "#445D48",
		},
	},
});

export default theme;
